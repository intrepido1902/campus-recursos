package com.example.campusrecursos.entities;

// Estados minimos exigidos por el tablero operativo (requisito 4.f.i):
// disponible, reservado, prestado o bloqueado.
public enum EstadoRecurso {
    DISPONIBLE,
    RESERVADO,
    PRESTADO,
    BLOQUEADO
}
